name: Release Binaries

on:
  push:
    tags:
      - 'v*'
    branches:
      - main
    paths:
      - 'version.sh'
  workflow_dispatch:

permissions:
  contents: write

jobs:
  build:
    runs-on: ubuntu-latest
    strategy:
      matrix:
        include:
          - goos: linux
            goarch: amd64
            name: linux-amd64
            ext: ''
          - goos: linux
            goarch: arm64
            name: linux-arm64
            ext: ''
          - goos: linux
            goarch: '386'
            name: linux-386
            ext: ''
          - goos: linux
            goarch: arm
            goarm: '7'
            name: linux-arm
            ext: ''
          - goos: windows
            goarch: amd64
            name: windows-amd64
            ext: .exe
          - goos: windows
            goarch: '386'
            name: windows-386
            ext: .exe
          - goos: darwin
            goarch: amd64
            name: darwin-amd64
            ext: ''
          - goos: darwin
            goarch: arm64
            name: darwin-arm64
            ext: ''
          - goos: linux
            goarch: arm64
            name: android-arm64
            ext: ''
          - goos: linux
            goarch: arm
            goarm: '7'
            name: android-arm
            ext: ''
          - goos: linux
            goarch: amd64
            name: android-x86_64
            ext: ''
    steps:
      - uses: actions/checkout@v4

      - name: Set up Go
        uses: actions/setup-go@v5
        with:
          go-version: '1.24'

      - name: Read version
        id: version
        run: |
          source version.sh
          echo "version=$VERSION" >> $GITHUB_OUTPUT

      - name: Build ${{ matrix.name }}
        run: |
          OUT_NAME="omnidrive-${{ matrix.name }}${{ matrix.ext }}"
          CGO_ENABLED=0 GOOS=${{ matrix.goos }} GOARCH=${{ matrix.goarch }} GOARM="${{ matrix.goarm }}" \
            go build -trimpath -ldflags "-s -w -X main.version=${{ steps.version.outputs.version }}" -o "$OUT_NAME" ./cmd/omnidrive

      - name: Upload artifact
        uses: actions/upload-artifact@v4
        with:
          name: omnidrive-${{ matrix.name }}
          path: omnidrive-${{ matrix.name }}${{ matrix.ext }}

  apk:
    runs-on: ubuntu-latest
    continue-on-error: true
    steps:
      - uses: actions/checkout@v4

      - name: Set up Go
        uses: actions/setup-go@v5
        with:
          go-version: '1.24'

      - name: Set up JDK
        uses: actions/setup-java@v4
        with:
          java-version: '17'
          distribution: 'temurin'

      - name: Set up Android SDK
        uses: android-actions/setup-android@v3

      - name: Read version
        id: version
        run: |
          source version.sh
          echo "version=$VERSION" >> $GITHUB_OUTPUT
          echo "code=$VERSION_CODE" >> $GITHUB_OUTPUT

      - name: Build APK
        run: |
          chmod +x android/build-apk.sh
          VERSION=${{ steps.version.outputs.version }} VERSION_CODE=${{ steps.version.outputs.code }} bash android/build-apk.sh

      - name: Upload APK
        uses: actions/upload-artifact@v4
        with:
          name: omnidrive-apk
          path: build/*.apk

  release:
    needs: [build, apk]
    if: always() && needs.build.result == 'success'
    runs-on: ubuntu-latest
    steps:
      - uses: actions/checkout@v4

      - name: Read version
        id: version
        run: |
          source version.sh
          echo "version=$VERSION" >> $GITHUB_OUTPUT

      - name: Download all artifacts
        uses: actions/download-artifact@v4
        with:
          path: artifacts
          merge-multiple: true

      - name: Create Release
        uses: softprops/action-gh-release@v2
        with:
          tag_name: v${{ steps.version.outputs.version }}
          files: artifacts/*
          generate_release_notes: true
        env:
          GITHUB_TOKEN: ${{ secrets.GITHUB_TOKEN }}
